import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pregadorbiblico.app',
  appName: 'Pregador Biblico',
  webDir: 'out'
};

export default config;
